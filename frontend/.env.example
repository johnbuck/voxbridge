# VoxBridge Frontend Environment Configuration
# Copy this file to .env and adjust values as needed

# ==============================================================================
# API CONFIGURATION
# ==============================================================================

# Backend API URL (HTTP)
# Development: http://localhost:4900
# Production: https://your-voxbridge-api.com
VITE_API_URL=http://localhost:4900

# Backend WebSocket URL
# Development: ws://localhost:4900
# Production: wss://your-voxbridge-api.com
VITE_WS_URL=ws://localhost:4900

# ==============================================================================
# LOGGING CONFIGURATION (Tiered System)
# ==============================================================================

# Global Log Level
# Controls default verbosity for all frontend modules
# Options: TRACE, DEBUG, INFO, WARN, ERROR
# TRACE - Ultra-verbose debugging (raw data, audio chunks, every iteration)
# DEBUG - Detailed debugging (checkpoints, state changes, WebSocket events)
# INFO  - Standard operational messages (connections, completions) [RECOMMENDED]
# WARN  - Warnings (recoverable errors, reconnection attempts)
# ERROR - Errors only (exceptions, failures)
VITE_LOG_LEVEL=INFO

# Per-Module Log Level Overrides
# Override global log level for specific modules
# Uncomment and adjust as needed for troubleshooting

# WebRTC logging (useWebRTCAudio hook, microphone, Opus encoding)
# VITE_LOG_LEVEL_WEBRTC=DEBUG

# WebSocket logging (connection events, message flow)
# VITE_LOG_LEVEL_WEBSOCKET=DEBUG

# UI logging (page components, state management)
# VITE_LOG_LEVEL_UI=DEBUG

# API client logging (HTTP requests, responses)
# VITE_LOG_LEVEL_API=DEBUG
